-- 緊急復旧用：RLS を無条件に無効化し、他アプリのアクセスを回復します
-- 他のアプリがこの profiles テーブルを共有している場合、RLS が有効になると適切なポリシーがない限りアクセスできなくなります。
-- まず、以下のコマンドを実行してサービスを復旧させてください。

ALTER TABLE public.profiles DISABLE ROW LEVEL SECURITY;

-- これで他アプリのログインが復旧するはずです。
-- dm_role カラム自体はそのまま残して問題ありません。
